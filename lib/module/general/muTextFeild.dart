import 'package:flutter/material.dart';

class MuTextField extends StatelessWidget {
  const MuTextField({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
